# Excel Integration Architecture - Visual Diagrams

---

## 1. Current State (Duplicated Code)

```
┌─────────────────────────────────────────────────────────────┐
│                      Flow Diagram Page                      │
└────────────────┬──────────────────────────┬─────────────────┘
                 │                          │
                 ↓                          ↓
        ┌────────────────────┐    ┌─────────────────────┐
        │ EditFlowDialog     │    │ ExcelSetupDialog    │
        ├────────────────────┤    ├─────────────────────┤
        │ _populate_excel    │    │ _on_browse_excel    │
        │ _options()         │    │ _on_auto_map_all()  │
        │                    │    │                     │
        │ ❌ DUPLICATED:     │    │ ❌ DUPLICATED:      │
        │ Get sheets from    │    │ Get sheets from     │
        │ Excel             │    │ Excel              │
        │ Get columns       │    │ Get columns        │
        │ Match flow names  │    │ Match flow names   │
        └────────────────────┘    └─────────────────────┘
                 │                          │
                 └──────────────┬───────────┘
                                ↓
                    ┌───────────────────────┐
                    │  FlowVolumeLoader     │
                    ├───────────────────────┤
                    │ _load_sheet()         │
                    │ get_flow_volume()     │
                    │ (Read-only, can't     │
                    │  create columns)      │
                    └───────────────────────┘
                                ↓
                        ┌───────────────┐
                        │  Excel File   │
                        │  .xlsx        │
                        └───────────────┘

PROBLEM: Code duplication across dialogs
         No way to create new columns
         Must edit Excel externally
```

---

## 2. Proposed State (Centralized)

```
┌──────────────────────────────────────────────────────────────┐
│                    Flow Diagram Page                         │
└────────────┬────────────────────────────┬────────────────────┘
             │                            │
             ↓                            ↓
    ┌─────────────────────┐      ┌──────────────────────┐
    │ EditFlowDialog      │      │ ExcelSetupDialog     │
    ├─────────────────────┤      ├──────────────────────┤
    │ Uses ExcelManager   │      │ Uses ExcelManager    │
    │ (no duplicate code) │      │ (no duplicate code)  │
    │                     │      │                      │
    │ Buttons:            │      │ Buttons:             │
    │ - Sheet selector    │      │ - Browse Excel       │
    │ - Column selector   │      │ - Auto-Map           │
    │ - Auto-Create ✨    │      │ - Save Mapping       │
    └─────────────────────┘      │ - Preview Widget ✨  │
             │                   └──────────────────────┘
             │                            │
             └────────────┬───────────────┘
                          ↓
        ┌─────────────────────────────────┐
        │   ExcelManager (NEW)            │
        │   (CENTRALIZED HUB)             │
        ├─────────────────────────────────┤
        │ ✅ get_sheets()                 │
        │ ✅ get_columns_for_sheet()      │
        │ ✅ create_column() ← NEW        │
        │ ✅ auto_map_flow()              │
        │ ✅ get_volume()                 │
        │ ✅ validate_excel()             │
        │ ✅ set_excel_path()             │
        │                                 │
        │ - Cache sheet/column lists      │
        │ - Check file modification time  │
        │ - Centralized error handling    │
        └────────────┬────────────────────┘
                     │
             ┌───────┴────────────────┐
             ↓                        ↓
    ┌──────────────────┐   ┌─────────────────────┐
    │ FlowVolumeLoader │   │ ExcelPreviewWidget  │
    │ (Read volumes)   │   │ (Display/Edit) ✨   │
    └──────────────────┘   │                     │
             │             │ - QTableWidget      │
             │             │ - Double-click edit │
             │             │ - Add rows          │
             │             │ - Save button       │
             │             └─────────────────────┘
             └────────────┬─────────────────────┘
                          ↓
                ┌────────────────────┐
                │   Excel File       │
                │   .xlsx            │
                │                    │
                │ Can now:           │
                │ ✅ Read data       │
                │ ✅ Create columns  │
                │ ✅ Edit cells      │
                │ ✅ Preview in-app  │
                └────────────────────┘

BENEFIT: No duplication, all operations centralized
         Can create columns, edit data
         Never need external Excel
```

---

## 3. Data Flow: Creating a New Flowline

### Before (Current - Manual)

```
User draws flowline: BH → Sump
        ↓
EditFlowDialog opens
        ↓
Manually select sheet: "Flows_UG2 North"
        ↓
Manually select column: ??? (doesn't exist!)
        ↓
❌ Need to create column manually
        ↓
Close app → Open Excel → Add header → Add rows → Save
        ↓
Return to app → Reload → Select column again
        ↓
Finally created! 😞
```

### After (Proposed - Automated)

```
User draws flowline: BH → Sump
        ↓
EditFlowDialog opens
        ↓
✅ ExcelManager.auto_map_flow("BH", "Sump")
   → Searches for "BH_to_Sump" column → Not found
        ↓
✅ Suggest column name: "BH_to_Sump" (auto-generated)
        ↓
✅ User clicks "Auto-Create Column"
        ↓
✅ ExcelManager.create_column("Flows_UG2 North", "BH_to_Sump")
   - Opens Excel file
   - Finds next empty column
   - Writes header: "BH_to_Sump"
   - Creates empty data rows
   - Saves file
   - (1-2 seconds)
        ↓
✅ Mapping created, dialog closes
        ↓
Done! 🎉
```

---

## 4. Data Flow: Loading Flow Volumes

### After Volumes Loaded

```
FlowDiagramPage._on_load_excel_clicked()
        ↓
ExcelManager.update_edge_volumes(year=2025, month=2)
        ↓
For each edge in diagram_data['edges']:
  - Get excel_mapping: {sheet, column}
  - Call ExcelManager.get_volume(sheet, column, 2025, 2)
  - Update edge['volume'] with returned value
        ↓
Re-render edges with volume labels
        ↓
Display complete! ✅
```

---

## 5. Component Interaction Diagram

```
┌────────────────────────────────────────────────────────────┐
│                      UI Layer                               │
├────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐ │
│  │ FlowDiagram  │    │ EditFlow     │    │ ExcelSetup   │ │
│  │   Page       │    │   Dialog     │    │   Dialog     │ │
│  └──────────────┘    └──────────────┘    └──────────────┘ │
│         │                    │                     │        │
│         │                    │                     │        │
└─────────┼────────────────────┼─────────────────────┼────────┘
          │                    │                     │
          └────────────────────┼─────────────────────┘
                               │
┌──────────────────────────────┼──────────────────────────────┐
│                    Service Layer                            │
├──────────────────────────────┼──────────────────────────────┤
│                              │                              │
│                    ┌──────────▼──────────┐                 │
│                    │  ExcelManager       │                 │
│                    │  (Singleton)        │                 │
│                    │                     │                 │
│                    │  get_sheets()       │                 │
│                    │  get_columns()      │                 │
│                    │  create_column()    │                 │
│                    │  auto_map_flow()    │                 │
│                    │  get_volume()       │                 │
│                    │  validate_excel()   │                 │
│                    │                     │                 │
│                    └──────────┬──────────┘                 │
│                               │                             │
│  ┌────────────────────────────┴───────────────────────┐   │
│  │                                                      │   │
│  ▼                                                      ▼   │
│ ┌──────────────┐    ┌──────────────┐    ┌──────────────┐ │
│ │FlowVolumeLoader│  │ExcelPreviewWidget│ │ (other      │ │
│ │(Read volumes)│  │(Display/Edit) │    │services)   │ │
│ └──────────────┘    └──────────────┘    └──────────────┘ │
│                                                            │
└────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────┼────────────────────────────┐
│                   Data Layer                             │
├─────────────────────────────┼────────────────────────────┤
│         ┌────────────────────▼──────────────────────┐   │
│         │    Excel File (openpyxl/pandas)          │   │
│         │    .xlsx                                  │   │
│         │                                           │   │
│         │  - Sheets: Flows_UG2N, Flows_Merensky... │   │
│         │  - Columns: BH_to_Sump, Sump_to_Plant... │   │
│         │  - Data: Year/Month/Volume cells         │   │
│         └───────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 6. Sprint Timeline

```
┌──────────────────────────────────────────────────────────────┐
│ Sprint 1: Foundation (4-6 hours)                             │
├──────────────────────────────────────────────────────────────┤
│  ✓ Create ExcelManager service                               │
│  ✓ Implement get_sheets(), get_columns(), validate()         │
│  ✓ Refactor dialogs to use manager (eliminate duplication)  │
│  ✓ Add unit tests                                            │
│  Result: Clean architecture, no duplicate code               │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ Sprint 2: Auto-Column Creation (6-8 hours) ⭐ BIG WIN        │
├──────────────────────────────────────────────────────────────┤
│  ✓ Implement ExcelManager.create_column()                    │
│  ✓ Add "Auto-Create Column" button to EditFlowDialog         │
│  ✓ Auto-generate column names: BH_to_Sump                   │
│  ✓ Create empty data rows matching Excel structure           │
│  Result: Users never touch external Excel for new columns   │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ Sprint 3: Excel Preview Widget (6-8 hours)                   │
├──────────────────────────────────────────────────────────────┤
│  ✓ Create ExcelPreviewWidget (QTableWidget-based)            │
│  ✓ Add cell editing (double-click)                           │
│  ✓ Add "Add Row" button for new months                       │
│  ✓ Integrate into ExcelSetupDialog (right pane)              │
│  ✓ Add mini-preview to EditFlowDialog                        │
│  Result: Users can view and edit Excel data in-app           │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ Sprint 4: Polish (3-4 hours)                                 │
├──────────────────────────────────────────────────────────────┤
│  ✓ Thread long Excel operations                              │
│  ✓ Add progress dialogs                                      │
│  ✓ Handle file lock errors gracefully                        │
│  ✓ Cache performance optimization                            │
│  ✓ Full integration testing                                  │
│  Result: Production-ready, smooth experience                 │
└──────────────────────────────────────────────────────────────┘

Total: 19-26 hours (2-3 weeks part-time)
```

---

## 7. Feature Comparison: Before vs After

| Feature | Before | After |
|---------|--------|-------|
| **Create New Columns** | ❌ Manual in Excel | ✅ Auto in app |
| **Edit Flow Data** | ❌ External Excel | ✅ In-app table editor |
| **Preview Columns** | ❌ Open Excel | ✅ Embedded widget |
| **Auto-Map Flows** | ⚠️ Partially (in setup) | ✅ Everywhere (unified) |
| **Code Duplication** | ❌ Yes (2+ places) | ✅ No (centralized) |
| **User Never Leaves App** | ❌ No | ✅ Yes |
| **Workflow Steps** | 10 steps | 6 steps |
| **Time Per Flowline** | 2-5 mins | 30 seconds |

---

## 8. Risk Mitigation Strategy

```
RISK                      MITIGATION
─────────────────────────────────────────────────────────
File locked (Excel open)  → Check permissions, show warning
Corrupted Excel structure → Validate before operations
Performance (large file)  → Cache + file mtime check
Data loss (bad save)      → Backup before modify, explicit save
Wrong column created      → User can customize name, validate
File not writable         → Catch PermissionError, guide user

All handled at ExcelManager level (centralized)
```

---

## 9. Technology Stack

```
Frontend (PySide6):
  - QDialog: EditFlowDialog, ExcelSetupDialog
  - QTableWidget: ExcelPreviewWidget (lightweight, cross-platform)
  - QProgressDialog: Feedback during operations
  - QThread: Background Excel operations

Backend (Python):
  - pandas: Read Excel data (lightweight, cached)
  - openpyxl: Write Excel (create columns, modify structure)
  - pathlib: File path handling
  - logging: Debug/error tracking

Cross-Platform:
  ✅ Windows, macOS, Linux (QTableWidget approach)
  ⚠️ Future: QAxWidget for Windows-only native Excel
```

---

**Next:** Proceed with Sprint 1? 🚀

